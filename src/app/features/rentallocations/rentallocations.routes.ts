import { Routes } from "@angular/router";
import { APPROUTES } from "../../app.routes.strings";
import { RentalLocationsComponent } from "./rentallocations.component";
 
export const RENTALLOCATIONS_ROUTES: Routes = [{ path: APPROUTES.rentallocations, component: RentalLocationsComponent }];
